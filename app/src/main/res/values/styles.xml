<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
    </style>

    <style name="MainFAb" parent="Widget.Design.FloatingActionButton">
        <item name="android:src">@drawable/search_to_close</item>
        <item name="backgroundTint">@color/colorPrimary</item>
    </style>

    <style name="M2Button" parent="Widget.AppCompat.Button">
        <item name="android:background">@drawable/button_background</item>
        <item name="android:textColor">@color/button_color_selector</item>
    </style>

</resources>
